; Deliberately loose. Verbose errors are part of the lesson: a broken quote in
; the search box drops the failing SQL straight into the page.
display_errors = On
display_startup_errors = On
error_reporting = E_ALL
expose_php = On
session.cookie_httponly = On
